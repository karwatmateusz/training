dictionary = {
    "klucz": "asd",
    "cos": "tam",
    "ala": "ma",
    "kot": "krotkie",
    "pies": "dlugie",
}

for key in dictionary:
    dictionary[key] = len(dictionary[key])

print(dictionary)
